# Telegram-бот с долгой памятью (RAG)

Telegram-бот, который работает с документами через векторную базу данных ChromaDB и отвечает на вопросы, используя технологию RAG (Retrieval-Augmented Generation).

## 🎯 Возможности

- 📄 **Загрузка документов** - поддержка PDF, TXT, DOCX
- 🧠 **Векторная база данных** - ChromaDB для хранения эмбеддингов
- 🔍 **Семантический поиск** - находит релевантные фрагменты в документах
- 💡 **Точные ответы** - AI отвечает только на основе загруженных документов
- 👥 **Мультипользовательский** - каждый пользователь имеет свою базу документов
- 💾 **Persistent storage** - документы сохраняются между перезапусками

## 🏗️ Архитектура

### Технологии
- **aiogram 3.x** - асинхронный фреймворк для Telegram
- **OpenAI Embeddings** - создание векторных представлений текста
- **OpenAI ChatCompletion** - генерация ответов на основе контекста
- **ChromaDB** - векторная база данных для хранения эмбеддингов

### Как работает RAG

1. **Загрузка документа** → Конвертация в текст
2. **Разбиение на чанки** → Части по 500 символов с перекрытием
3. **Создание эмбеддингов** → OpenAI text-embedding-3-small
4. **Сохранение в ChromaDB** → Векторная база данных
5. **Вопрос пользователя** → Создание эмбеддинга запроса
6. **Семантический поиск** → Поиск похожих фрагментов
7. **Генерация ответа** → GPT с найденным контекстом

## 📦 Установка

### 1. Клонируйте репозиторий или создайте проект

### 2. Создайте виртуальное окружение (рекомендуется)

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Настройте переменные окружения

Создайте файл `.env` в корне проекта:

```env
BOT_TOKEN=ваш_токен_telegram_бота
OPENAI_API_KEY=ваш_ключ_openai
OPENAI_MODEL=gpt-3.5-turbo
EMBED_MODEL=text-embedding-3-small
```

### 5. Запустите бота

```bash
python bot_long_memory.py
```

## 🔑 Получение токенов

### Telegram Bot Token

1. Найдите [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен в `.env`

### OpenAI API Key

1. Зайдите на [platform.openai.com](https://platform.openai.com/)
2. Перейдите в раздел [API Keys](https://platform.openai.com/api-keys)
3. Создайте новый ключ (Create new secret key)
4. Скопируйте ключ в `.env`

## 🎮 Использование

### Команды бота

- `/start` - Запустить бота и увидеть инструкции
- `/info` - Информация о загруженных документах
- `/clear` - Очистить память (удалить все документы)

### Рабочий процесс

1. **Запустите бота**: `/start`

2. **Загрузите документ**:
   - Отправьте файл (PDF, TXT, DOCX)
   - Дождитесь обработки (создание эмбеддингов)

3. **Задайте вопрос**:
   - Напишите вопрос по документу
   - Получите точный ответ на основе содержимого

4. **Продолжайте диалог**:
   - Задавайте дополнительные вопросы
   - Загружайте новые документы

### Пример использования

```
Вы: [Отправляете PDF с договором]
Бот: ✅ Документ успешно обработан!
     📄 Файл: contract.pdf
     📊 Размер: 15000 символов
     🧩 Фрагментов: 32

Вы: Какая сумма указана в договоре?
Бот: 💡 Ответ:
     В договоре указана сумма 500 000 рублей...

Вы: Какой срок действия?
Бот: 💡 Ответ:
     Срок действия договора составляет 12 месяцев...
```

## ⚙️ Настройки

В файле `bot_long_memory.py` можно изменить:

### Размер чанков
```python
CHUNK_SIZE = 500      # Размер фрагмента (символы)
CHUNK_OVERLAP = 50    # Перекрытие между фрагментами
```

### Количество результатов поиска
```python
n_results=3  # В функции retrieve_context()
```

### Модель OpenAI
```python
OPENAI_MODEL = "gpt-3.5-turbo"  # или "gpt-4"
```

### Модель эмбеддингов
В файле `.env`:
```env
EMBED_MODEL=text-embedding-3-small  # или text-embedding-3-large
```

### Температура генерации
```python
temperature=0.3  # Низкая = точнее, высокая = креативнее
```

## 📁 Структура проекта

```
Bot_with_Memory/
├── bot_long_memory.py      # Основной файл бота
├── bot_short_memory.py     # Бот с короткой памятью
├── requirements.txt        # Зависимости
├── .env                    # Переменные окружения (создайте сами)
├── memory/                 # База данных ChromaDB (создается автоматически)
│   └── chroma.sqlite3
└── README_LONG_MEMORY.md   # Этот файл
```

## 🔧 Функции кода

### `load_document(file_content, filename)`
- Загружает и конвертирует документ в текст
- Поддерживает TXT, PDF, DOCX

### `split_text_into_chunks(text, chunk_size, overlap)`
- Разбивает текст на части с перекрытием
- Обеспечивает целостность контекста

### `embed_chunks(user_id, chunks, filename)`
- Создает эмбеддинги через OpenAI
- Сохраняет в ChromaDB с метаданными

### `retrieve_context(user_id, query, n_results)`
- Выполняет семантический поиск
- Возвращает N наиболее релевантных фрагментов

### `answer_question(query, context_items)`
- Формирует промпт с контекстом
- Генерирует ответ через ChatCompletion

## 🐛 Решение проблем

### Ошибка: "ModuleNotFoundError"
```bash
pip install -r requirements.txt
```

### Ошибка: "BOT_TOKEN не найден"
Создайте файл `.env` с токенами (см. раздел Установка)

### Ошибка при обработке PDF
```bash
pip install --upgrade PyPDF2
```

### Ошибка при обработке DOCX
```bash
pip install --upgrade python-docx
```

### ChromaDB не создает папку
Проверьте права доступа к папке проекта

## 📊 Сравнение ботов

| Функция | bot_short_memory.py | bot_long_memory.py |
|---------|---------------------|-------------------|
| Память | 10 последних сообщений | Неограниченно документов |
| Хранение | RAM (dict) | ChromaDB (persistent) |
| Поиск | Последовательный | Векторный (semantic) |
| Документы | ❌ | ✅ PDF, TXT, DOCX |
| Персистентность | ❌ | ✅ |
| Точность | Контекст диалога | Контекст документов |

## 📝 Требования

- Python 3.8+
- OpenAI API ключ (с балансом)
- Telegram Bot Token

## 🚀 Production советы

1. **Ограничьте размер файлов**:
```python
MAX_FILE_SIZE = 10 * 1024 * 1024  # 10 MB
```

2. **Добавьте rate limiting** для защиты от спама

3. **Используйте Redis** для кеширования эмбеддингов

4. **Мониторинг затрат** на OpenAI API:
   - text-embedding-3-small: $0.02 / 1M токенов
   - gpt-3.5-turbo: $0.50 / 1M токенов

5. **Backup базы данных** ChromaDB регулярно

## 📄 Лицензия

MIT License - используйте свободно!

## 🤝 Поддержка

Если возникли вопросы или проблемы, откройте Issue в репозитории.

---

Создано с ❤️ для работы с документами через AI
